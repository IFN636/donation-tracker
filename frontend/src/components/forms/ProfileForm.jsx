import { Button, Input } from "antd";

const ProfileForm = ({ formData, setFormData }) => {
    return (
        <div>
            <h2>Profile Form</h2>

        </div>
    );
};

export default ProfileForm;
